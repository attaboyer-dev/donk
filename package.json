{
  "name": "donk-mono",
  "private": true,
  "workspaces": [
    "src/frontend",
    "src/backend",
    "src/utils"
  ],
  "scripts": {
    "clean": "rm -rf src/*/dist",
    "build": "yarn workspaces foreach -v --all --parallel run build",
    "fe": "yarn workspace @donk/frontend dev",
    "be": "yarn workspace @donk/backend dev"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "typescript": "^5.8.3"
  },
  "license": "MIT",
  "packageManager": "yarn@4.9.2"
}