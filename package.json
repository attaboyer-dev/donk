{
  "name": "donk-mono",
  "private": true,
  "workspaces": [
    "src/frontend",
    "src/backend",
    "src/utils"
  ],
  "scripts": {
    "build": "yarn workspaces foreach --topological --recursive run build",
    "fe": "yarn workspace @donk/frontend dev",
    "be": "yarn workspace @donk/backend dev"
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  },
  "license": "MIT",
  "packageManager": "yarn@4.9.2"
}
